---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: octoeverywhere-neppro-config
spec:
  accessModes: ["ReadWriteMany"]
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-filesystem
